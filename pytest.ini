[pytest]
# Only the curated suite under tests/ runs in CI. The legacy top-level
# test_*.py scripts need live secrets / network and are NOT collected here.
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q
