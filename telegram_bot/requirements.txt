# JARVIS Telegram Bot — зависимости для VPS-деплоя (24/7)
# Лёгкий набор без десктопных зависимостей (PyQt6, sounddevice, selenium и т.д.)
# pip install -r telegram_bot/requirements.txt

# Gemini
google-genai>=1.0.0

# Telegram
python-telegram-bot>=20.0

# Mini App сервер
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
aiosqlite>=0.19.0

# WebSocket (мост бот ↔ ПК)
websockets>=12.0

# Долгая память: Postgres (Neon) для постоянного хранения на Render.
# Без DATABASE_URL используется SQLite (стирается при перезапуске Render).
asyncpg>=0.29.0

# Голосовые ответы: портативный ffmpeg для PCM→OGG/Opus (без системных пакетов)
imageio-ffmpeg>=0.5.1

# Погода в утреннем брифинге (Open-Meteo, без API-ключа)
httpx>=0.27.0

# Google Calendar (telegram_bot/gcal.py). Лежали только в корневом
# requirements.txt, который в образ не попадает: Dockerfile ставит ЭТОТ файл,
# поэтому календарь молча падал в проде с 19.06.2026 —
# «list_events failed: No module named 'googleapiclient'».
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.0.0
