__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.log
build/
dist/

# Секреты деплоя (Oracle/VPS) — НЕ коммитим
.env

# Конфиги с секретами и личными данными
config/api_keys.json

# Userbot (Telethon) сессия — ПОЛНЫЙ доступ к аккаунту, НИКОГДА не коммитить
config/userbot.session
config/userbot.session-journal
config/spotify_tokens.json
config/user_profile.json
config/calendar.json
config/translation_history.json
config/translation_context.json
config/usage_patterns.json
config/activity_tracking.json

# Память пользователя
memory/data.json
memory/current_mode.json

# Telegram Mini App — база памяти "второго мозга"
config/jarvis_memory.db
config/jarvis_memory.db-journal
*.sqlite
*.sqlite3

# Резервные копии (backup ротация)
*.bak
*.json.broken-*

# IDE
.vscode/
.idea/

# Виртуальные окружения
venv/
env/
.venv/

# OS
.DS_Store
Thumbs.db

# Google Calendar OAuth — never commit secrets / tokens
config/credentials.json
config/token.json

config/gcal_auth_url.txt

config/unlock_secret.txt

jarvis_watchdog.log
jarvis_pc_server.log

# Реплики стенда замера — генерируются офлайн за секунду (scripts/latency_probe.py)
logs/latency_voice/

# Кэш ключевого слова для бенчмарка — синтезируется системным TTS на месте
logs/benchmark/

# Черновики и одноразовые пробы — не код проекта
scratch/

# Образцы голоса для сравнения (генерируются на месте)
logs/voicecheck/
