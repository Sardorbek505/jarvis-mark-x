# JARVIS → личный двойник: план

## Цель
Превратить бота в секретаря / друга / советника / двойника.

## Слой 1 — Долгая память + досье  ← ГОТОВО ✅
- [x] memory_store.py — durable хранилище (Postgres через DATABASE_URL, fallback SQLite)
- [x] Досье: имя, о себе, цели, предпочтения + список фактов
- [x] RAM-кэш + инъекция в системный промпт Gemini (помнит при перезапуске)
- [x] Авто-обучение: после диалога фоном вытаскивать новые факты и сохранять
- [x] Команды: /profile, /remember <факт>, /forget
- [x] Подключить и в Telegram, и в Mini App (общая память)
- [x] asyncpg в requirements
- [x] Тесты: дедуп, персистентность при «перезапуске» — пройдены

## Слой 2 — Режимы личности / советник  ← ГОТОВО ✅
- [x] /mode ментор|друг|бизнес|обычный — меняет характер
- [x] personas.py — оверлей личности поверх базового промпта
- [x] Хранить выбранный режим в досье (колонка mode + миграция)
- [x] Подключено в Telegram и Mini App (общий контекст)
- [x] Тесты: set/get/resolve/overlay, миграция старой БД — пройдены

## Слой 3 — Календарь и задачи  ← ГОТОВО ✅
- [x] Таблица tasks в durable-БД (память), общий стор
- [x] agenda.py — NL-парсер дат (завтра/сегодня/в HH:MM/DD.MM/день недели)
- [x] Команды /task, /tasks, /today, /done
- [x] Сортировка: события с датой первыми, todo без даты — в конце
- [x] Задачи инъектятся в контекст Gemini (спросить «что сегодня?» в чате)
- [x] Тесты: парсинг, сортировка, today-фильтр, done — пройдены

## Слой 4 — Проактивный секретарь  ← ГОТОВО ✅
- [x] proactive.py — loop, утро (8–11ч) и вечер (22–24ч) по таймзоне юзера
- [x] gemini.generate_once — брифинг с памятью/задачами/персоной, без истории
- [x] Дедуп через durable meta-таблицу (не дублирует при перезапуске)
- [x] Команды-превью /morning, /evening
- [x] Loop в bot.py и render_app
- [x] Тесты: окна времени, дедуп, утро/вечер/день — пройдены

---

## Ревью
Бот-двойник собран из 4 слоёв на едином durable-хранилище (Postgres через
DATABASE_URL, fallback SQLite):
1. Долгая память + досье + авто-обучение фактам
2. Режимы личности (ментор/друг/бизнес) поверх базовой личности
3. Задачи/календарь с NL-парсингом дат, видимые ассистенту в контексте
4. Проактивный секретарь: сам пишет утром и вечером

Принципы соблюдены: минимальное воздействие (один стор, переиспользование
context_provider и _system_for), без костылей, миграции безопасны, всё
покрыто прогонами. Для вечной памяти на Render — один шаг: DATABASE_URL (Neon).

---

## Доп. фичи (по запросу пользователя)
- [x] Онбординг-знакомство при первом /start
- [x] Голос везде: голос→голос (PCM→OGG/Opus через портативный ffmpeg), текст→текст
- [x] Погода в утреннем брифинге (Open-Meteo, нужен allowlist хостов)
- [x] Inline-кнопки (режимы/задачи/привычки, callback-хендлер)
- [x] Трекер привычек (/habit, /habits, /check, серии 🔥)
- [ ] «Напиши маме» — голосовое контакту (бот представляется ассистентом), нужен канал доставки

---

## Obsidian «мозг» (голосовой Джарвис, 2026-07-15)
- [x] Vault `C:/Users/User/Documents/JarvisVault` (00-Inbox/Daily/Knowledge/Projects + README)
- [x] Путь в `config/obsidian.json` (не хардкод)
- [x] `actions/obsidian.py` → `obsidian_action`: write / append_daily / search / read / list
- [x] Инструмент `obsidian` в TOOLS + ветка в `_execute_tool` + импорт (main.py)
- [x] `test_obsidian.py` — все 5 действий зелёные, файлы в UTF-8
- [ ] (опц.) Проактивная подгрузка последних заметок в system prompt
- [ ] (опц.) Obsidian Local REST API + MCP — доступ к vault вне сессии Джарвиса

**Ревью:** минимальное воздействие — добавлен один экшен по существующему паттерну
(сигнатура `x_action(parameters, player)`), атомарная запись .md, переиспользован
`core.storage.safe_read_json`. Голосом: «запиши…», «добавь в дневник…»,
«что я записывал про…», «прочитай заметку…», «покажи заметки».

---

## Аудит: мусор, ошибки, слабые места (2026-08-06)

Разбор `_execute_tool` и смежного кода по мандату «найди корень, не ходи слепо».
Каждая находка проверена запуском, а не чтением.

### Память — почему ответы были мимо
- [x] Догадки ассистента оседали в досье как факты о пользователе.
      Замер: «привет, как дела?» + реплика про BEK STYLE → факт «работает в
      компании BEK STYLE». Промпт извлечения получил правило источника.
- [x] Недоставленный проактивный вопрос записывал следующую фразу как ответ
      («Возраст: включи музыку»). Побочные эффекты — только после доставки.
- [x] Слова пользователя подавались модели под подписью «ты говорил это раньше».
- [x] `/facts` + `/forget <номер>` — убрать один неверный факт, не стирая всё.
      Голое `/forget` больше не стирает память.

### Сломанное наглухо
- [x] Перевод: 3 из 6 действий писали в несуществующие ключи JSON
      (`enabled_languages` → KeyError всегда; `default_language` → в никуда,
      с рапортом об успехе). Операции переехали в translation_manager.
- [x] События Google Calendar уезжали на 2 часа: зашитая зона `Europe/Moscow`
      при владельце в Шымкенте (UTC+5).
- [x] `team_collaboration`: код читал `description` и `assignee`, модели их не
      объявили — проекты навсегда без описания, задачи без исполнителя.

### Лаги и хрупкость
- [x] `translate_text` ходил в сеть прямо в событийном цикле, который гонит
      микрофон в Live API. Блокирующих вызовов в async теперь ноль.
- [x] Успех отправки определялся словом «Отправлено» в тексте ответа ПК.
      Теперь структурный `ok`; для очереди цена ошибки была — повторная
      отправка адресату при каждом коннекте.
- [x] Синхронизация с Google запускалась по слову «добавил» в ответе.
- [x] SQLite-фолбэк валил трейсбек ожидаемой миграции при каждом запуске.

### Осталось
- [ ] Перезапустить ПК-клиент, чтобы `ok` шёл структурно с обеих сторон
      (сейчас работает через запасной путь)
- [ ] Проверить `/facts` на проде — в досье могли остаться выдуманные факты
- [ ] Активационное слово «Джарвис» на десктопе (выбор владельца, не начато)
- [ ] Groq Whisper STT — нужен ключ
- [ ] Отозвать токен бота через @BotFather (он в истории git до 2f782d1)

**Ревью:** тесты 123 → 179. Каждый дефект закреплён тестом, который падал бы
на прежнем коде; три ложные тревоги моих же скриптов отброшены до отчёта
(немые обработчики, дрейф ключей конфига, «решения по прозе» в Spotify).

### Второй круг (действия и доставка)
- [x] Из нескольких блоков одного типа исполнялся только первый: блоки
      вырезаются через sub (все), а исполнялись через search (один). Три
      задачи из просьбы превращались в одну, следа не оставалось нигде.
- [x] Доставка напоминаний жила двумя копиями (bot.py и render_app.py);
      отметка «доставлено» стояла в одном try с отправкой.
- [x] Отправка контакту считалась удачной по слову «Отправлено» в тексте.
- [x] Google Calendar: зашитая Europe/Moscow, молчащие сбои, запуск
      синхронизации по слову «добавил» в ответе.

---

## Живучесть ПК-моста (2026-08-17)

Разбор `jarvis_pc_server.log` за 05–17.08 (233 записи, 69 обрывов) скриптом,
а не глазами. Медиана простоя — **2 секунды**, то есть в норме мост здоров.

### Отброшено как ложная тревога
Четыре самых страшных «простоя» — не отказы моста, а **сон ноутбука**.
События Kernel-Power 506/507 совпадают с ними до секунды:

| «Простой» в логе | Событие Windows | Что это на самом деле |
|---|---|---|
| 06.08 10:18 → 20:43 (10 ч) | 506 в 10:18:05, 507 в 20:43:02 | сон |
| 08.08 08:58 → 19:26 (10 ч) | 506 в 08:58:58, 507 в 19:26:44 | сон |
| 16.08 20:13 → 20:53 (41 мин) | 506 в 20:13:02, 507 в 20:53:40 | сон |
| 05.08 17:40 → 18:29 (49 мин) | — | хвост июльского простоя (HTTP 503) |

Спящий ПК недоступен по определению. Итоговые «7.99% недоступности» —
артефакт того, что лог не отличает сон от аварии.

### Настоящий дефект
Три окна при работающем ноутбуке: 16.08 15:27→15:35 (461 с), 16.08 15:38→15:41
(173 с), 17.08 13:29→13:34 (262 с). В каждом — по две записи в логе и
многоминутная тишина между ними.

Корень **не установлен, и это главное**: у `websockets.connect` таймаут
открытия по умолчанию 15 с и оборачивает всё, включая TCP-коннект, а
наблюдаемые попытки жили 53 с и ~200 с. Значит либо событийный цикл клиента
не получает управления (Modern Standby душит фоновые процессы, голодание CPU),
либо таймаут не срабатывает. Обёртывать это ещё одним `wait_for` бессмысленно —
он сломается по той же причине. Сначала замер.

- [x] Скрипт разбора лога: причины, окна простоя, медиана
- [x] Сверка окон простоя с журналом Kernel-Power — сон отделён от аварий
- [x] Длительность каждой попытки в лог (сейчас затянувшаяся не видна вообще)
- [x] Различать сон и заморозку процесса, не считать их отказом моста
- [x] Явный `open_timeout` вместо молчаливого умолчания
- [x] Тесты на новое поведение
- [ ] Вернуть задачу «JARVIS PC Watchdog» — упирается в UAC, нужен владелец

### Фабрика дублей — найдена и закрыта

Гонясь за обрывами, наткнулся на то, что оказалось важнее их. Сторож
`pc_watchdog.ps1` определял живость клиента так:

```powershell
Where-Object { $_.CommandLine -like "*telegram_bot.pc_server*" }
```

`CommandLine` у клиента **пустой**: его запускает задача автозапуска с
RunLevel Highest, а непривилегированный запрос не читает командную строку
elevated-процесса. Сторож видел «мёртв» каждую минуту и поднимал ещё одну
копию. Копии дрались за мост, и 17.08 одна из них провисела семь минут,
не написав в лог ни строки, — тот самый симптом «ПК офлайн при живом ПК».

- [x] Живость по замку (порт 47821), а не по `CommandLine` —
      `Get-NetTCPConnection` пассивен и не зависит от elevation
- [x] Замок в самом клиенте: вторая копия выходит с кодом 3, не мешая первой.
      Порт, а не lock-файл — упавший процесс освобождает его сам
- [x] `start_pc.bat` по коду 3 гасит лишний цикл перезапуска, а не крутит его вечно

### Проверено вживую, а не только тестами
- Клиент с новым кодом поднимается за 1.6–1.8 с, `/health` → `pc:true`
- Вторая копия при живой первой: выход с кодом 3, первая не тронута
- Сторож при живом клиенте: ничего не делает (exit 0, в логе пусто)
- Сторож при убитом клиенте: поднял ровно одну копию, записал строку в лог,
  мост вернулся (`pc:true`)

### Осталось владельцу
- Задача «JARVIS PC Watchdog» не регистрируется без прав администратора
  («Access is denied»). Нужен один запуск из админского PowerShell:
  `powershell -ExecutionPolicy Bypass -File scripts\install_watchdog.ps1`
- Цепочка автозапуска флапает сама по себе: `wscript.exe` из задачи
  «JARVIS PC Server» дважды подряд стартовал и не порождал `cmd` (задача
  Running, boot-лог пуст). Вручную тот же `.vbs` и `.bat` отрабатывают.
  Сторож раз в минуту делает эту флапающую цепочку неважной — ещё одна
  причина завести задачу выше.

---

## Задержка голоса: Джарвис не отвечал вовсе (2026-08-17)

Задача звучала «почему долго отвечает». Оказалось хуже: на дефолтных
настройках он **не отвечал ни на одну реплику** — расшифровывал сказанное
и молчал. То, что иногда ответ всё же приходил, зависело от шума в комнате.

### Корень
Конец фразы определяет VAD на стороне Gemini, и определить его он может
только по ПОЛУЧЕННОЙ тишине. Микрофонный гейт (`MIC_RMS_THRESHOLD`) тишину
не пропускает, экономя квоту, а хвост после речи был 10 кадров = 0.64 с при
окне VAD 400 мс — без запаса. Сервер оставался ждать продолжения фразы.

Замер на стенде, одни и те же три реплики:

| Хвост тишины | Ответов из 3 |
|---|---|
| 0.64 с (было) | **0** |
| 1.0 с (стало) | 2–3, стабильно |
| 1.92 с | 3 |

### Сделано
- [x] Хвост выведен из окна VAD с запасом (`MIC_HANGOVER_MS = VAD + 600`),
      а не подобран на глаз магическим числом
- [x] Точка отсчёта замера — только громкий кадр. Раньше отсчёт вёлся и от
      кадров хвоста, из задержки вычиталась его длина, и «слышит» выходило
      отрицательным — цифра лучше правды
- [x] `scripts/latency_probe.py` — стенд: речь синтезирует офлайн-голос
      Windows (Microsoft Irina), Gemini настоящий, динамики в никуда.
      Замер стал воспроизводимым и не требует человека у микрофона
- [x] Оба дефекта закреплены тестами

### Цифры после правки (медиана по ходам)
```
отвечает 3432 мс · худшая 4566 мс   (звучит совпадает — вывод ни при чём)
```

### Грабли стенда (не повторять)
Первая версия `_FakeMic` в паузах между фразами не отдавала кадров вообще.
Настоящая звуковая карта сыплет их ровным потоком независимо от того, говорит
человек или нет. Из-за этого гейт с хвостом не вызывался, стенд мерил
несуществующее поведение, и я успел сделать неверный вывод «дело не в VAD».

### Второй корень: модель ДУМАЛА перед каждым ответом

Владелец предложил перевести десктоп на Fish Audio 2.5 («отвечает меньше
секунды»). Прежде чем менять архитектуру — замер, из чего состоят ~2 с модели:
раздумья или синтез речи. Один и тот же звук, один системный промпт, три
прогона на конфиг, первый байт аудио:

| Конфиг | Медиана |
|---|---|
| как было | **4152 мс** |
| `thinking_budget=0` | **1377 мс** |

Ответ на TEXT-модальности снять не вышло: `native-audio` её не поддерживает
(`1007 — requested combination of response modalities (TEXT)`). Заодно это
объясняет вчерашний 1007 в логе.

Вывод: тормозил не синтез, а рассуждения. **Fish Audio эту задержку не лечит
в принципе** — в нашем тракте отдельной стадии TTS нет, модель синтезирует
сама. Разговорной реплике и вызову инструмента рассуждения не нужны.

- [x] `thinking_config=ThinkingConfig(thinking_budget=0)`, ручка
      `JARVIS_THINKING_BUDGET` — поднять, если начнёт путаться в многошаговых
      просьбах

### Итог по задержке

| Этап | «отвечает», медиана |
|---|---|
| Было (17.08 утро) | не отвечал вовсе |
| Хвост тишины починен | 3432 мс |
| Раздумья выключены | **2440 мс** |

Цифры сходятся: 1377 мс изолированно + 1000 мс нашего хвоста ≈ 2440 мс.
Ход с реальным вызовом погоды (702 мс) уложился в 2485 мс.

### Голос Джарвиса от Fish на десктопе (2026-08-17)

Владельцу важен не темп, а ТЕМБР: у Fish голос звучит как настоящий Джарвис,
и это сделало его фаворитом. На десктопе стоял пресет Gemini «Charon».

Схема: мозг остаётся прежним — Gemini Live понимает речь, держит характер и
вызывает инструменты. Меняется только, кто произносит готовый ответ. Текст
берём из `output_transcription`; попросить у Live-модели ответ ТЕКСТОМ нельзя —
`1007` и у `2.5-native-audio`, и у `3.1-flash-live` (проверено). Значит Gemini
всё равно синтезирует Charon'а, и этот звук мы выбрасываем.

Провайдер один и тот же, что у Telegram-бота (`telegram_bot/tts_fish.py`,
модель `s2.1-pro-free`, голос `680d74fb…`) — второго не заводим.

Замер форматов Fish (фраза 70 символов, с машины владельца):

| Формат | Первый кусок | Весь файл |
|---|---|---|
| `wav` + `balanced` | **989 мс** | 2221 мс |
| `wav` + `normal` | 3548 мс | 4120 мс |
| `pcm` + `normal` | 1858 мс | 2233 мс |

`wav` на 24 кГц = ровно частота вывода десктопа, поэтому ffmpeg не нужен.
Opus отпадает: он не умеет 24 кГц, только 48.

- [x] `speak_pcm()` в общем провайдере + снятие RIFF-заголовка поиском чанка
      `data` (длина заголовка не фиксирована)
- [x] Переключатель `JARVIS_VOICE` (`fish` по умолчанию, `gemini` — вернуть Charon)
- [x] Звук Gemini выбрасывается, когда говорит Fish — закреплено тестом
      (два голоса на одну реплику хуже любого из них)
- [x] Ход закрывается, когда зазвучал Fish, а не когда сервер прислал
      `turn_complete`: текст готов раньше, чем человек что-то слышит
- [x] Живьём: «Голос Fish: 3.9 с звука на 54 символов», «6.8 с на 102 символа»

**Задержку этого тракта честно измерить пока не вышло.** Прогоны дали 48 мс
и 11060 мс — обе цифры мусорные: в первом случае ответ пришёл уже после
начала следующей реплики стенда, во втором, похоже, сказался троттлинг
бесплатной квоты после десятка прогонов за день. Нужен спокойный замер.

- [ ] Достоверный замер задержки с голосом Fish (стенд, `PROBE_GAP_SEC`)
- [ ] Стриминг: сейчас `speak_pcm` ждёт ВЕСЬ файл, поэтому длинный ответ
      платит целиком. Отдавать Fish по предложению и играть по мере готовности

---

## HUD: анимации в стиле Старка (2026-08-17)

Запрос — анимации интерфейса «строго как у Тони Старка». Рендер выбран
QPainter (существующий `HudCanvas`), а не веб-холст в QWebEngineView:
WebEngine у нас есть, но переписывать ради свечения незачем — главная беда
HUD была не в отсутствии эффектов.

### Корень: HUD врал

Вся «реакция» строилась на `random.uniform()` — холст дышал одинаково в
тишине и на крике, а полоска-осциллограф рисовала `random.randint(3, 20)`.
Это заставка, а не интерфейс. У Старка HUD реагирует на реальность.

Оба настоящих сигнала уже были в коде и просто не использовались:
RMS микрофона считается в `_is_loud_enough()`, амплитуда собственного голоса
доступна в `_play_audio` до записи в устройство.

- [x] `HudCanvas.level` 0..1 с быстрой атакой и плавным спадом — как у
      стрелочного индикатора; цели пересчитываются каждый кадр, а не
      выдумываются раз в полсекунды
- [x] Осциллограф внизу — бегущая история громкости (36 столбиков ≈ 0.6 с),
      а не случайные числа
- [x] Искры летят гуще на громких словах
- [x] Дыхание в тишине оставлено намеренно: мёртвый HUD читается как
      выключенный, а не как спокойный
- [x] Прицел на инструмент: янтарные скобки смыкаются к ядру (ease-out) и
      подписываются именем модуля — «WEATHER», «SPOTIFY». Джарвис у Старка
      никогда не работает молча
- [x] Сигналы `_level_sig`/`_tool_sig` — громкость идёт из аудио-потока,
      трогать виджеты оттуда напрямую нельзя
- [x] `set_level`/`lock_on` добавлены и в `HeadlessUI`: их зовут на каждом
      кадре, и «нет такого метода» уронило бы микрофон
- [x] Тест: тишина даёт ровно 0, громкий кадр поднимает волну выше 0.9
- [x] Проверено глазами — 7 состояний сняты в PNG

Две шкалы намеренно разные: `JARVIS_LEVEL_SCALE` (микрофон, 4000) и
`JARVIS_SPEAK_LEVEL_SCALE` (синтез, 9000). У синтеза громкость ровнее и
выше, по микрофонной шкале волна упиралась бы в потолок на каждом слове.

### Не сделано из предложенного
- [ ] Бут-последовательность: каркас прочерчивается сам (`line drawing` +
      `stagger` + `typewriter` на строках статуса)
- [ ] `ДУМАЕТ`: сегментированное кольцо-прогресс с дискретным щелчком
- [ ] `RECONNECTING`: сдвиг в красный, джиттер, скан-линия помех
- [ ] Метрики CPU/RAM: перекатывающиеся моноширинные цифры

### Дальше — ручной сигнал конца речи
Из 3.4 с примерно 1.4 с уходит на ожидание тишины: наш хвост 1.0 с плюс окно
VAD 400 мс. Клиент уже ЗНАЕТ, когда речь кончилась (гейт по RMS), но
выбрасывает это знание и заставляет сервер выводить его заново. SDK умеет
иначе: `automatic_activity_detection(disabled=True)` +
`send_realtime_input(activity_start=..., activity_end=...)` — проверено,
поля есть. Риск: слишком ранний `activity_end` заставит перебивать на паузах
внутри фразы, поэтому нужен подбор на стенде, а не с ходу.

- [ ] Ручной сигнал активности вместо ожидания тишины (ожидаемо −1.0…1.4 с)
- [ ] Метрика «слышит» бессмысленна для потоковой расшифровки: фрагменты
      приходят, пока человек ещё говорит, поэтому значение отрицательное.
      Мерить от НАЧАЛА реплики либо убрать

**Проверено и НЕ является дефектом** (чтобы не проверять заново):
- Часовые пояса напоминаний: `parse_reminder` сохраняет tz, «через 10 минут»
  в 14:50 (+05) → 10:00 UTC при текущем 09:50 UTC. Верно.
- «напомни завтра в 9» не разбирается быстрым путём, но это не тупик:
  фраза уходит к модели, та выдаёт [[REMINDERS]] с явной датой.
- Сравнение времён строкой: формат везде наивный UTC ISO, порядок корректен.
- `enabled_languages` — единственный дрейф ключей конфига во всём проекте.

---

## Разбор Mark-LIV (FatihMakes) — 2026-09-18

Полный отчёт: `docs/MARK_LIV_ANALYSIS.md` (все 24 инструмента с параметрами,
14 подсистем `core/`, данные и конфиги, вердикт по каждому пункту).

Работа исследовательская, кода не менялось. Главное, что вскрылось — три вещи
у нас сломаны, и увидели мы это только при сравнении:

- [x] `session_resumption` был включён в конфиге, но `new_handle` нигде не
      читался — каждый реконнект начинал пустую сессию. Хендл подхватывается
      в `_receive_audio`, возвращается в `_build_config`, сбрасывается
      `_drop_stale_handle()`, если сервер его не принял.
- [x] Гейт необратимых действий. Уточнение к первой редакции: гейт у нас БЫЛ
      (`_DESTRUCTIVE` в `main.py`), утверждение «подтверждения нет вовсе» было
      неверным. Настоящий дефект другой: подтверждением считался повторный
      вызов инструмента, который модель делает сама, а поле
      `_pending_destructive` не инициализировалось — первое же «выключи
      компьютер» падало с AttributeError в приёмном цикле. Теперь токен выдаёт
      интерфейс (`core/confirm.py` + баннер в `ui.py`), в headless остаётся
      прежнее правило.
- [x] `memory/memory_manager.py` кладёт в промпт всю память целиком: добавлены
      бюджет, индекс `[ТАКЖЕ ПОМНЮ]`, `search_memory()` за инструментом
      `recall_memory`, `forget()` и предохранитель без молчаливого удаления.

Следом по ценности:
- [x] Стек undo (`core/undo.py`) + корзина ОС вместо безвозвратного удаления.
      В стек кладутся только настоящие обратные действия: абсолютная громкость
      и яркость на Windows выставляются от догадки, и отмена возвращала бы
      выдумку.
- [x] Верхние границы версий и платформенные маркеры в `requirements.txt`
      (заодно объявлены `pycaw`/`comtypes`, которые импортировались, но в
      зависимостях не значились)
- [ ] `web_search`: наш DuckDuckGo Instant Answer — заглушка, нужен
      Gemini Grounded + режимы `search|news|research|price|compare`
- [ ] `action_loader`: `TOOL` dict в каждом `actions/*.py` вместо цепочки
      `if/elif` на 300 строк в `main.py`; отсюда же `{tools}`/`{limits}` в промпт

Решено НЕ брать: аватар с виземами (~1340 строк чужой визуальной подписи),
`game_updater`, `flight_finder`, `dev_agent`, телефонный dashboard — и никакого
копирования кода: Mark-LIV под CC BY-NC 4.0, мы под MIT.


### Замечено попутно, не чинилось (вне объёма уровня A)

- [x] `actions/morning_briefing.py:_load_memory()` читал `memory/data.json`
      плоским `get("name")`, хотя имя лежит в `identity/name` — брифинг всегда
      здоровался «сэр» и всегда определял город по IP. Починено.
- [ ] `tests/test_hotkey_manager.py` падает на Linux (поток хоткеев
      Windows-специфичный). Падало и до этих изменений — в CI на Windows
      зелено, на локальной проверке в контейнере нет.

### Уровень B — сделано

- [x] B6: `web_search` — Gemini с Google Search, разбор выдачи DuckDuckGo,
      Instant Answer, браузер последним и без притворства, что это ответ.
      Режимы search/news/research/price/compare.
      **Не проверено живьём:** контейнер не пускает ни к DuckDuckGo, ни к Google.
      Разбор выдачи проверен на сохранённой разметке, переходы — на подменах.
- [x] B7: `core/action_loader.py` — инструмент объявляет себя сам через `TOOL`
      в своём модуле. 14 инструментов переехали, `main.py` короче на 430 строк.
      10 встроенных остались: они вплетены в состояние живой сессии.
- [x] B8: `{tools}`, `{limits}`, `{os}` в промпте — список способностей
      собирается из живого реестра, а не из перечня, который разошёлся бы с кодом.

- [x] B9: `actions/system_monitor.py` — инструмент `system_status`. Чего машина
      не измеряет, о том не говорит: ноль градусов вслух звучит как показание.
- [x] B10: `actions/os_reminder.py` — напоминание через планировщик ОС,
      срабатывает при закрытом ДЖАРВИСЕ. Отказ планировщика не выдаётся за успех.
- [x] B11: `core/audio_devices.py` + окно выбора. Хранение по имени, не по
      индексу: индексы сдвигаются при подключении наушников.
      **Не сделано из задуманного:** измерение устройств (проверка, что host API
      реально играет) — для этого нужен стенд с железом, в контейнере
      звуковых устройств нет вовсе.
- [x] B12: `core/push_to_talk.py` — микрофон закрыт, пока не держат Ctrl+Space.
      На Windows опрос VK-кодов, на macOS/Linux — события клавиш окна, и модуль
      прямо говорит, что там режим работает только в фокусе.

### Найдено попутно и починено

- [x] `core/calendar_manager._parse_datetime` объявлена `Optional` и все три
      вызывающих места проверяют результат на None — но None был недостижим:
      любая непонятная фраза доезжала до «сегодня в 9:00». «Напомни
      когда-нибудь» молча ставило напоминание на девять утра.

### Уровень C — сделано

- [x] C13: `actions/file_processor.py` + перетаскивание файла в окно.
      `_on_file` в ui.py существовал, но его никто не вызывал — бросить файл
      было некуда. Отсутствие пакета отключает один формат, а не инструмент,
      и сообщение называет, что поставить.
- [x] C14: `core/browser_session.py` — Playwright в отдельном потоке с очередью
      команд (синхронный API привязан к своему потоку, а действия идут из
      произвольных потоков executor'а). Нажатие по видимой надписи, а не по
      селектору: человек голосом называет надпись. **Проверено вживую** —
      Chromium в контейнере есть.
- [x] C15: `actions/youtube_video.py` — пересказ ролика по субтитрам, не
      открывая его. Тренды сознательно не сделаны: без ключа Data API их
      пришлось бы выскребать из разметки главной страницы.
- [x] C16: 43 действия в `computer_control` вместо девяти, все названы в
      объявлении, опечатки чинит difflib локально.
- [x] C17: плагины — `plugins/*.py` с объявлением `PLUGIN`, выключение через
      `plugins_disabled` в настройках.

### Найдено попутно и починено (уровень C)

- [x] Обрезка «по последнему пробелу» отсекала последнее слово и у текста,
      который обрезать не требовалось: «Здесь написано важное» превращалось в
      «Здесь написано…». Было в `file_processor` и в `youtube_video`.

### Не проверено живьём (нужен ваш запуск на Windows)

- [ ] Поиск через Gemini + Google Search и разбор выдачи DuckDuckGo: сетевая
      политика контейнера не пускает ни туда, ни туда
- [ ] Планировщики задач: `schtasks`, `launchctl`, `systemd-run` — команды
      собраны и проверены тестами, но не выполнялись
- [ ] Выбор аудиоустройств и push-to-talk: в контейнере нет ни звуковых
      устройств, ни Windows

### По отдельной просьбе: запуск игр

- [x] `actions/game_launcher.py` — «включи ведьмака». `open_app` ищет по PATH,
      а игры там не лежат, поэтому голосом они не запускались вообще.
      Список читается с диска: `appmanifest_*.acf` и `libraryfolders.vdf` у
      Steam, JSON-манифесты у Epic. Запуск — ссылкой лаунчера, не через .exe:
      игре нужен живой лаунчер (античит, облачные сохранения, активация).
- Сознательно НЕ взято из `game_updater` Mark-LIV: обновление и установка
  (гигабайты по чужой воле) и закрытие игры процессом (несохранённый прогресс).
  Это те 1101 строка, которые в разборе отмечены как «не брать».
- [ ] Проверить на машине с играми: путь Steam из реестра и разбор
      `libraryfolders.vdf` с настоящими экранированными путями Windows.

## Уровень D — остаток разбора, по убыванию пользы

- [x] D1 (пункт 10 списка): мгновенное подтверждение перед долгим
      инструментом — `core/acknowledge.py`. Поиск в сети, разбор PDF,
      пересказ ролика и запуск браузера занимают секунды, и всё это время
      снаружи тишина: неотличимо от «не расслышал». Теперь звучит короткая
      фраза («Секунду, ищу»), настоящий ответ встаёт в очередь звука следом.
      Существенное в решении:
      - фразы не обещают результата (это проверяется тестом): «Нашёл» до
        конца поиска опровергнет сам же ответ через три секунды;
      - быстрые инструменты молчат — подтверждение к действию на двести
        миллисекунд ложится поверх ответа;
      - если модель уже что-то сказала в этом ходе, подтверждения нет:
        иначе человек услышит два «сейчас посмотрю» подряд;
      - браузер подтверждается, только пока не открыт: Chromium поднимается
        секунды, клик по открытой странице — доли;
      - замер задержки фраза не закрывает (`метрики=False` у `_speak_fish`),
        иначе в статистику попадали бы полсекунды вместо настоящих четырёх и
        медленные инструменты перестали бы быть заметны;
      - с голосом самого Gemini подтверждения нет: чужой голос вперемешку с
        его собственным хуже полутора секунд ожидания.
- [x] D2 (пункт 5 списка): пять действий `files`, которых не было.
      - `append` — дописать в конец. `create_file` перезаписывает файл
        целиком, поэтому «добавь в список молоко» давало список из одного
        молока. Отмена — обрезкой до прежней длины, а не возвратом снимка:
        точно и работает с файлом любого размера.
      - `info` — размер, дата, тип; для папки — сколько внутри.
      - `largest` — что занимает место. Обход ограничен 50 000 файлами и
        честно сообщает, что был прерван: домашняя папка бывает больше, а
        минута тишины выглядит как поломка.
      - `find` по расширению. Прежний `*pdf*` находил и папку «pdf-скрипты»,
        и файл «pdf_инструкция.txt» — всё, кроме того, что просили.
      - `organize` — раскладка по «Картинки/Документы/Музыка/Видео/Архивы/
        Программы». Ярлыки не трогаются (перенесённый ярлык ломает привычку
        открывать программу со стола), папки не переезжают, незнакомые
        расширения остаются на месте, занятые имена не перезаписываются.
        Вся раскладка отменяется одним «отмени» — поэтому баннер
        подтверждения ей не нужен: гейт в этом проекте стоит на
        необратимом.
- [x] D3 (пункт 1 списка): `core/watcher.py` + `actions/topic_watch.py` —
      «следи за темой и скажи, когда появится новое». 30 инструментов.
      Существенное — не в том, что Джарвис заговаривает сам, а в том, когда
      он молчит:
      - «новое» — это не «ответ поиска изменился»: поиск переформулирует одно
        и то же каждый раз. Каждое предложение сравнивается с уже сказанным
        нестрого (rapidfuzz). Порог 80 выбран по замеру: пересказ той же
        новости даёт 78–96, разные новости по теме — около 50;
      - отсюда честная граница, записанная в описание инструмента: это
        слежение за СОБЫТИЯМИ, а не за третьим знаком в курсе валют;
      - первая проверка идёт сразу и уходит в ответ (человек и так спросил),
        заодно становясь точкой отсчёта — иначе через полчаса Джарвис объявил
        бы новостью то, что сам же и рассказал;
      - пять тем, не чаще раза в 15 минут, максимум две новости за раз и ни
        слова с 23 до 8. Проверка по прямой просьбе работает и ночью;
      - список и запомненное лежат в `~/.jarvis/watch/topics.json`: иначе
        «следи за этим» живёт до первого закрытия окна, и человек узнаёт об
        этом молчанием.
- [x] D4 (пункт 3 списка): панель памяти в окне — кнопка «🧠 ПАМЯТЬ».
      `all_entries()` и `forget()` были написаны ещё под индекс памяти и
      ждали применения: увидеть накопленное можно было только в
      `memory/data.json`, а забыть голосом — лишь помня, КАК факт назван.
      Список сужается по мере набора, каждая строка убирается крестиком.
      - `filter_entries()` — подстрока, а не `_score`: человек в поле фильтра
        сужает список буква за буквой, и ранжирование выглядит как пропажа
        строк;
      - категория передаётся в `forget` обязательно: один ключ живёт в двух
        категориях, и забыть чужое молча хуже, чем не забыть ничего;
      - подтверждения нет: нажатие на крестик и ЕСТЬ решение человека, гейт
        в этом проекте стоит на том, что делает модель;
      - строки при перерисовке отвязываются от родителя, а не только
        `deleteLater`: иначе убранная из раскладки строка остаётся
        нарисованной до следующего прохода цикла событий.
- [x] D5 (пункт 6 списка): `actions/messenger.py` — написать человеку в
      WhatsApp или Telegram. 31 инструмент.
      - Переписка открывается с готовым текстом, ОТПРАВКУ нажимает человек.
        Дожать Enter технически можно, но проверить, что открылся тот чат и
        что фокус в поле ввода, нечем: неотправленное сообщение человек
        отправит сам за секунду, отправленное не тому не отзывается никак.
        В ответе прямо сказано, что не отправлено, и тест это стережёт.
      - Записная книжка в `~/.jarvis/contacts.json`: голосом номер не
        диктуют, говорят «напиши маме».
      - Номер не угадывается: из десяти цифр без кода страны не понять,
        российский он или узбекский. Единственное исключение — привычная
        восьмёрка вместо +7, и использованный номер называется вслух, чтобы
        ошибка была видна сразу.
      - Падежи: «маме» находит «маму» отсечением хвостовых гласных, а не
        нечётким сравнением — «Дима» и «Дина» похожи ровно настолько же,
        насколько «Дима» и «Диме».
- [x] D6 (пункт 7 списка): `actions/desktop.py` — обои и «сверни всё».
      32 инструмента. Раскладка стола по папкам уже сделана в D2 (files
      action=organize), поэтому здесь осталось только то, чего не было.
      - Обои ставятся по картинке, брошенной в окно: путь называть не надо.
        Ради этой связки всё и затевалось.
      - Прежние обои читаются у системы ДО замены, и «отмени» возвращает
        именно их. Не прочитались — отмена не регистрируется, и об этом
        сказано вслух: отмена, ставящая наугад выбранную картинку, хуже её
        отсутствия.
      - В GNOME 42+ ставится и `picture-uri-dark`: одна светлая картинка
        означала бы возврат прежних обоев при переключении темы, то есть
        «не сработало» с точки зрения человека.
      - «Сверни всё» на Linux честно говорит про отсутствие wmctrl, а на
        Windows проверяет, что сочетание отправилось.
- [x] D7 (пункт 8 списка): `actions/code_runner.py` — запустить скрипт и
      разобрать код без запуска. 33 инструмента.
      - Запуск проходит тот же гейт, что выключение компьютера: это
        единственное действие во всём наборе, выполняющее ПРОИЗВОЛЬНЫЙ код,
        и отмены у него нет.
      - Попутно закрыта дыра в самом гейте: `_is_destructive` сверял
        действие со списком опасных, поэтому ПРОПУЩЕННЫЙ параметр `action`
        проходил мимо подтверждения. Для code_runner заведена обратная
        таблица `_DESTRUCTIVE_UNLESS`: опасно всё, кроме явного `check`.
      - `check` разбирает Python через `compile()` — находит ошибку с
        номером строки и не выполняет ни одной. Тест это стережёт: скрипт,
        который создал бы файл, его не создаёт.
      - Написания кода здесь НЕТ: модель пишет текст сама, а кладёт его в
        файл `files` с action=create_file. Второй способ делать то же самое
        не нужен, и описание отправляет модель туда.
- [x] D8 (пункт 9 списка): буфер обмена — `core/clipboard.py` +
      `actions/clipboard_tool.py`. 34 инструмента.
      **Сделано НЕ панелью, и это сознательное отступление от исходного
      пункта.** У Mark-LIV панель всплывает при каждом копировании с
      кнопками «перевести / пересказать / объяснить». Следить за буфером —
      значит читать всё, что человек копирует: пароли из менеджера паролей,
      коды из банковских сообщений, чужую переписку. Ассистент, который
      видит это постоянно, отличается от ассистента, который видит это по
      просьбе, ровно тем, что первому нельзя доверять буфер вообще.
      Сценарий сохранён целиком: «переведи, что я скопировал» работает —
      инструмент отдаёт текст, а переводит, пересказывает и объясняет модель
      сама. Поэтому действий два (read/write), а не пять кнопок.
      - На Windows чтение и запись через ctypes: без возни с кодировкой
        консоли powershell.
      - На Linux wl-clipboard проверяется РАНЬШЕ xclip: в сеансе Wayland
        xclip работает с XWayland, то есть мимо настоящего буфера.
      - Нет ни одного средства — так и говорится, с именем недостающего:
        пустая строка была бы неотличима от пустого буфера.
- [x] D9 (пункт 11 списка): `core/llm_client.py` — один вход к текстовой
      модели с очередью провайдеров Gemini → Ollama → OpenAI-совместимый.
      Пересказ файла и пересказ ролика идут через него.
      **Сделана только текстовая часть плана `tasks/provider-layer.md`, и
      это осознанно.** Голосовой ход живёт внутри Live API; вынести его —
      значит построить второй турн-цикл с собственным VAD, ту самую тяжёлую
      часть, которую Mark-XLIX сам бросил. Обещать «оффлайн-режим», имея
      текстовый фолбэк, было бы враньём — в описании слоя так и написано.
      - Раньше это были три копии одного блока, и все три замолкали разом,
        когда у Gemini кончалась квота.
      - Живость Ollama кэшируется на минуту: иначе каждый пересказ платил бы
        таймаутом соединения за уже выясненное.
      - Поиск (`web_search`) оставлен со своим вызовом: ему нужен grounding
        Google Search, которого у других провайдеров нет.
- [x] D10 (пункт 12 списка): настройки разговора — `core/settings.py` +
      кнопка «⚙ РАЗГОВОР» в окне.
      - Окно VAD, захват начала фразы, размышления и показ замера задержки
        жили в переменных среды, то есть для человека не существовали. А
        жалобы на них самые частые: «перебивает» и «долго молчит» — это одна
        и та же ручка, повёрнутая не туда.
      - **Попутно закрыта ловушка, которую эта же настройка и создавала.**
        Хвост тишины микрофона обязан перекрывать окно VAD, иначе Gemini не
        дожидается тишины и НЕ ОТВЕЧАЕТ ВОВСЕ. Пока окно жило в переменной
        среды, инвариант держался руками; ползунок «пусть не перебивает»
        сделал бы Джарвиса немым, и связать одно с другим человек не смог бы
        никогда. Теперь `_sync_hangover` тянет хвост следом при каждой
        сборке конфигурации.
      - Значения читаются при сборке конфигурации, поэтому вступают в силу с
        ближайшим переподключением, а не с перезапуском.
      - Пояснение написано рядом с каждой ручкой: настройка, смысл которой
        надо где-то прочитать, не будет тронута никогда.
      - Внешнего вида HUD в настройках НЕТ сознательно: палитра реактора
        ничего не решает в работе, а сам HUD — наша опознаваемая черта, по
        той же причине, по которой мы не берём чужое лицо с виземами.
- [x] D11 (пункт 4 списка): `actions/screen_pointer.py` — найти на экране то,
      что названо словами, и работать с этим мышью. 35 инструментов.
      «Нажми Сохранить» было невыполнимо: мышью Джарвис не владел, а
      координаты голосом не диктуют. Снимок уходит в ту же модель зрения, но
      с другой задачей — вернуть рамку и НАДПИСЬ элемента.
      - Правило «сомневаешься — не нажимай». Клика не будет, если элемент не
        найден, если модель сама оценила уверенность как низкую или если
        прочитанная надпись не похожа на просьбу. Курсор при этом не
        двигается, а Джарвис говорит, что видит.
      - Сравнение надписи с просьбой — по СЛОВАМ, а не по буквам, порог
        выбран замером: «Удалить» и «Сохранить» побуквенно похожи на 67
        баллов, то есть побуквенное сравнение разрешало бы «нажми
        Сохранить» нажать «Удалить». По словам они расходятся до 50, а
        настоящие пары дают 100.
      - `find` и `move` безопасны и работают даже при низкой уверенности:
        отказ нужен перед нажатием, а не перед взглядом.
      - Рамка вне экрана отвергается: модель иногда отвечает не в той шкале.
- [x] D12 (пункт 2 списка): `core/session_log.py` — «вчера мы говорили о…»
      в брифинге и по прямому вопросу (`morning_briefing` action=yesterday).
      - Долгосрочная память хранит ФАКТЫ, разговор она не хранила вовсе, и
        каждое утро Джарвис начинал с чистого листа.
      - Запись дешёвая, выжимка ленивая: реплики дописываются в файл дня по
        ходу разговора (миллисекунды, переживает закрытие крестиком), а
        выжимка считается при первом вопросе — один вызов модели в сутки
        вместо одного на реплику. На выходе из программы модель просто не
        успела бы ответить.
      - «Прошлый день» — последний день С РАЗГОВОРОМ, а не буквально вчера:
        между разговорами бывают выходные, и «вчера мы говорили» про день,
        когда компьютер не включали, звучало бы ложью.
      - Журналы старше двух недель удаляются: расшифровки разговоров — не
        то, что стоит копить бесконечно, а важное к тому времени уже лежит
        в памяти фактов.
      - Пустая строка здесь нормальна и часта (первый день, молчаливая
        неделя, разговор из двух реплик) — брифинг выглядит целым и без неё.

### Итог уровня D

Все 12 пунктов списка закрыты. 35 инструментов, 1035 тестов.
Одно сознательное отступление: пункт 9 (буфер обмена) сделан инструментом,
а не всплывающей панелью — причина записана в D8.
Одно сознательное сужение: пункт 11 (провайдерный слой) сделан только для
текстовых задач — причина записана в D9 и в tasks/provider-layer.md.

## Уровень E — долги, а не новые возможности

- [x] E1: `scripts/selfcheck.py` — самопроверка: что на ЭТОЙ машине работает
      по-настоящему. Больше половины сделанного в уровне D нельзя проверить
      тестами дальше, чем «логика вокруг системного вызова верна»: обои, мышь,
      буфер, планировщик, Steam, WhatsApp, живой поиск.
      - По умолчанию только чтение. Самопроверка, которая сама что-то
        испортила, бесполезна вдвойне: её запускают как раз тогда, когда уже
        не уверены, что всё цело.
      - `--полная` — обратимые проверки действием: буфер получает обратно своё
        содержимое, курсор — прежнюю точку, задача планировщика удаляется
        по своему тексту (чужие напоминания не трогаются).
      - `--сеть` отдельно: запрос тратит квоту ключа.
      - Четыре отметки, и разница между ними — вся ценность: OK, НЕТ (нечем
        работать, и это ожидаемо), СБОЙ (всё на месте, но не работает),
        МИМО. Код возврата поднимает только СБОЙ.
      - Отличает «нет графического сеанса» от «снимок не сделался»: на сервере
        в контейнере это не поломка, которую надо чинить.
      - Попутно нашла настоящую дыру: на Linux при живом `systemd-run` может
        не быть `notify-send`, и напоминание ставится, но не показывается.
        Теперь об этом сказано в отчёте с командой установки.
      - Попутно починен уровень лога: `ensure_gemini_key(interactive=False)`
        писал ERROR на штатной ветке «ключа нет, а спрашивать не просили» —
        её зовут поиск, пересказ и слой моделей на каждый запрос.
- [x] E2: красный тест в каждом прогоне. `test_hotkey_manager` безусловно
      требовал Windows-поведения от `start()`, который вне Windows сознательно
      ничего не делает. Разделён на две ветки со `skipif`, и к ним добавлено
      то, чего не проверял никто: что вне Windows `stop()` без `start()`
      безопасен (его зовёт `cleanup()` всегда) и что повторный `start()` не
      плодит слушателей. **Прогон стал полностью зелёным: 1048 тестов.**
- [x] E3: README описывал ассистента, которого больше нет. Семь строк
      возможностей, примеры команд про Obsidian и громкость, бейдж «274
      теста» — ни слежения, ни мыши, ни мессенджера, ни игр, ни браузера, ни
      чтения документов, ни панели памяти. Человек, пришедший в репозиторий,
      не узнавал и половины из 35 инструментов.
      - Таблица возможностей переписана под то, что есть на самом деле.
      - Добавлен раздел «Чего Джарвис не делает»: подтверждение кнопкой,
        неотправленные за вас сообщения, отказ нажимать при сомнении,
        отсутствие слежки за буфером. Это характер проекта, и он объясняет
        половину решений в коде лучше, чем список фич.
      - Примеры команд заменены на живые, включая перетаскивание файла.
      - Самопроверка описана рядом с тестами: что проверяют тесты, а что —
        только запуск на настоящей машине.
- [x] E4: `main.py` разгружен там, где следующая ошибка была бы дорогой.
      - `_receive_audio` 145 → 64 строки. Из цикла чтения сокета вынуто то,
        что к чтению отношения не имеет: `_on_user_said` (эмоции, инициатива,
        обучение, прогноз), `_on_jarvis_said`, `_play_model_audio`,
        `_catch_resume_handle`, `_run_tool_calls`.
      - `_execute_tool` 311 → 102 строки. Диспетчер десяти встроенных
        инструментов уехал в `_run_inline_tool`, обновление профиля — в
        `_note_activity`. «Это не мой инструмент» возвращается отдельным
        объектом `_НЕ_ИНЛАЙН`: пустая строка или None значили бы «инструмент
        отработал и промолчал», и реестр действий не получил бы управления.
      - Поведение не менялось ни в одном месте: 1048 тестов как были.
      - Зато появилось 17 новых — на то, что раньше нельзя было спросить
        отдельно: неизвестное имя не выдаётся за успех, упавший инструмент
        не рвёт приём, «посмотрел погоду» не пишется человеку в занятия,
        а включение без названия не попадает в «недавно смотрел».
- [x] E5: `web_search` при отказе Gemini склеивал выдержки DuckDuckGo в
      «Заголовок. Выдержка Заголовок. Выдержка» — стену SEO-текста, в которой
      ответ есть, а найти его на слух нельзя. Теперь выдержки пересказывает
      текстовая модель через `llm_client`.
      - Прямой запрет досказывать: модель, которой отдали три строчки
        выдачи, охотно допишет остальное по памяти, и это будет выдумка с
        интонацией факта.
      - Gemini пропускается (`кроме=("gemini",)`): его только что спросили и
        он отказал, повторная попытка стоит ещё одного таймаута на пути,
        который и так деградировал. Локальная модель при этом сработает.
      - Некому пересказать — выдержки отдаются как есть: хуже связного
        ответа, но лучше молчания.
      - Режим (новости, цена, сравнение) доходит до пересказчика: потерять
        его здесь значит получить общий пересказ вместо ответа.
- [x] E6: инструменты стало можно выключать — кнопка «🧰 ИНСТРУМЕНТЫ».
      35 объявлений уходили в промпт на КАЖДОМ подключении, а сессия
      переподключается сама каждые несколько минут: тому, у кого нет ни
      Spotify, ни Obsidian, ни игр, это оплачивалось токенами и сбивало
      выбор — чем длиннее список, тем чаще модель берёт не тот инструмент.
      - Прежний `plugins_disabled` продолжает читаться: молча перестать его
        понимать значило бы вернуть человеку плагин, который он выключил.
      - Выключенное не загружается вовсе, а не «загружается и молчит»:
        модель не должна видеть способность, которой её лишили.
      - Четыре инструмента выключить нельзя даже правкой файла:
        `save_to_memory`, `recall_memory`, `undo`, `shutdown_jarvis`. В окне
        они показаны запертыми, а не спрятаны: иначе человек будет искать в
        окне то, чего там нет, и думать, что сломалось.
      - Вызов выключенного объясняется («выключен в настройках»), а не
        отрицается: сессия могла начаться до того, как его выключили.
      - Попутно: функция `settings.set` перекрывала встроенный тип и уронила
        весь импорт настроек строкой `set[str] = set()`. Переименована в
        `save`, урок записан.
