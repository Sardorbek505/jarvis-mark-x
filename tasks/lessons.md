# Уроки

## Безопасность
- Никогда не коммить файлы с реальными ключами. `config/api_keys.json` уже в `.gitignore` — но был добавлен в индекс ДО игнора. Всегда проверять `git ls-files` на наличие секретных файлов, даже если они в `.gitignore`.
- Не хардкодить CLIENT_ID/SECRET/TOKEN в `.py`-скриптах. Читать из config/env.
- Если секрет уже попал в историю git — игнора недостаточно, ключ нужно отозвать/перевыпустить.

## Структура репозитория
- Записи с режимом `160000` в `git ls-files -s` — это submodule-gitlinks. Без `.gitmodules` это битые ссылки/мусор.
- Одноразовые миграционные скрипты с абсолютными путями (`D:/...`) — удалять после применения.

## Windows: cp1251 роняет код в самый неподходящий момент
Консоль Windows по умолчанию cp1251. Любой `print()` с эмодзи (`✅`, `⚠️`) или
псевдографикой (`╔═╗`) падает с `UnicodeEncodeError`. Русский текст, тире и
многоточие — кодируются нормально, проблема только в символах вне cp1251.

Дважды за одну сессию это ударило именно по коду обработки ошибок:
`core/storage.py:safe_read_json` — код аварийного восстановления — печатал
`⚠️` и падал, роняя Джарвиса на битом конфиге вместо мягкого отката.

**Правило:** в пользовательский вывод — только ASCII-оформление (`[OK]`, `[!]`,
`===`), а лучше logger вместо print. Проверять под строгой консолью:
`sys.stdout = io.TextIOWrapper(io.BytesIO(), encoding="cp1251", errors="strict")`.

## Логгеры называются по-разному — массовые правки ломаются об это
В `actions/`, `core/`, `ui.py` логгер — `_logger`, в `telegram_bot/` — `logger`.
Скрипт массовой правки вписал везде `_logger` → `NameError` в 6 файлах, причём
падало бы ровно в момент поимки ошибки. Перед массовой заменой — проверять
фактическое имя в каждом файле:
`grep -E "^\s*\w+\s*=\s*logging\.getLogger"`.

## Тесты: `PermissionError` — это не про код
Симптом: все тесты падают с `PermissionError: [WinError 5]` на
`%LOCALAPPDATA%\Temp\pytest-of-User`. Причина не в проекте — сломан ACL самой
временной папки pytest (остаток от запуска из-под администратора). Признак:
даже `icacls`/`Get-Acl` не могут прочитать права.

Лечение (PowerShell от админа, синтаксис именно PowerShell — не `cmd`):
`Remove-Item -LiteralPath "C:\Users\User\AppData\Local\Temp\pytest-of-User" -Force -Recurse`
Pytest пересоздаст папку сам с правильным владельцем.

**Не лечить через `--basetemp` в `pytest.ini`:** pytest очищает эту папку при
каждом старте — фиксированный путь в конфиге это мина, а не решение.

## Молчаливые ошибки — главный источник долгоживущих багов
Было 60 обработчиков `except Exception: pass`. Из-за них баг «напоминание не
называет время» прожил месяцы: код не падал, а тихо ничего не делал.
При добавлении логов поток управления не менять — только видимость.
Уровень: `warning` для конфигов/состояния (тихий откат на дефолты = баг),
`debug` для цепочек фолбэков (иначе шум на каждом кадре).
В сообщение НЕ зашивать номер строки — `exc_info=True` даёт точную строку сам.

## `min_size=1` в пуле = серверless БД не засыпает и жрёт квоту
Джарвис лежал с 21 июля: Neon упёрся в квоту compute → `memory.init()` кидал
исключение → падал lifespan FastAPI → Space выходил с кодом 3 → ПК-клиент две
недели долбился в 503. Корень не в Neon: `asyncpg.create_pool(min_size=1)`
держит соединение вечно, поэтому база не уходит в autosuspend (5 мин простоя) и
жжёт ~180 CU-часов при лимите 100. Для serverless-БД всегда `min_size=0` плюс
`max_inactive_connection_lifetime`.

## Инфраструктурная зависимость не должна ронять приложение
`await memory.init()` в lifespan без try/except убил весь бот из-за недоступной
БД. Деградация вместо смерти: Postgres → SQLite → режим «без памяти», громкий
лог, фоновый `watch()` возвращает основную базу сам, когда та оживёт. Проверять
вопросом: «если этот сервис умрёт, что именно перестанет работать — фича или
всё?»

## Ретрай без backoff — это не отказоустойчивость, а DDoS самого себя
Фиксированные 5 секунд дали 46 037 одинаковых строк и лог на 7 МБ. Нужны три
вещи сразу: экспоненциальный backoff с потолком, джиттер (чтобы клиенты не
синхронизировались) и подавление повторов — новая причина логируется сразу,
неизменная раз в N попыток.

## Тесты: незакрытый `aiosqlite` вешает pytest на выходе
Все тесты зелёные, но процесс не завершается. Причина — не-daemon поток внутри
`aiosqlite.connect()`: один store в тесте не закрыли. Симптом «78 passed, а
процесс висит» = утечка соединения, а не падение теста.

## Блокировку egress надо измерять, а не угадывать
HF перестал пускать не только к api.telegram.org (это знали), но и к
*.workers.dev — прокси, работавший до 21 июля, стал недостижим. При этом
vercel.com, deno.com, api.render.com и pypi.org из того же контейнера
доступны: блокировка точечная, а не «нет интернета». Прежде чем выбирать
новый хост для обхода — замерить, куда доступ есть (`DIAG_EGRESS=1` в
render_app). Один замер экономит несколько слепых деплоев.

## Переменная с именем «критические действия» ≠ защита
В main.py лежал словарь critical_actions с shutdown/restart/delete и
комментарий «may require confirmation». Кода подтверждения не было: писалась
строчка в лог и действие выполнялось. Читая бегло, видишь защиту — её нет.
Проверять не по названиям, а по тому, что происходит с управлением дальше.

## Забытый импорт в большом файле ловится только запуском
Дважды за сессию в main.py использовал os и time, которых там не было.
ast.parse проходит — это NameError, а не синтаксис. Дешёвая проверка:
сверить множество импортированных имён и реально загрузить модуль.

## Побочные эффекты — только после рискованной операции

`curiosity.pose()` помечал вопрос заданным до `bot.send_message`. Одного
сбоя DNS хватило, чтобы вопрос сгорел из банка, все будущие вопросы
заглохли, а следующая реплика пользователя легла в досье как ответ на
невидимый вопрос («Возраст: включи музыку») — навсегда и в каждый промпт.

Правило: сначала совершить то, что может не получиться, и только потом
записывать, что оно получилось. Если состояние приходится ставить заранее
(защита от долбёжки) — ставить МИНИМУМ и откатывать остальное.

## `git push space master` ≠ деплой

Space собирается с ветки `main`. `git push space master` создал новую
ветку `master`, отрапортовал успехом и не задеплоил ничего. Нужно
`git push space master:main`. Проверять `git ls-remote space` — на что
смотрит HEAD, а не верить слову «pushed».

## Лог, не отличающий сон от аварии, врёт про надёжность

Разбор `jarvis_pc_server.log`: 7.99% недоступности, два «простоя» по 10 часов.
Оба совпали до секунды с Kernel-Power 506/507 — ноутбук просто спал. Медиана
настоящего простоя оказалась 2 секунды, то есть мост здоров.

Спящий процесс видит только оборванный сокет и честно пишет «Отключено».
Отличить одно от другого можно лишь двумя шкалами сразу: при гибернации
монотонные часы стоят, а стенные идут; при Modern Standby идут обе, но попытка
живёт много дольше отпущенного ей таймаута. Пока это не различается, любая
метрика доступности — художественное произведение.

## Проверять живость процесса по `CommandLine` нельзя

`Get-CimInstance Win32_Process | Where CommandLine -like "*pc_server*"` возвращал
пустую строку для клиента, запущенного задачей с RunLevel Highest:
непривилегированный запрос не читает командную строку elevated-процесса.
Сторож из-за этого каждую минуту считал живого клиента мёртвым и поднимал
дубль — сам себе фабрика дублей, ровно то, от чего должен был защищать.

Живость проверять по ресурсу, который процесс реально держит (занятый порт,
`Get-NetTCPConnection -State Listen`), а не по описанию процесса. Заодно
единственный на машину замок — порт, а не lock-файл: упавший процесс
освобождает порт сам, залипший файл остаётся миной навсегда.

## Таймаут, который не сработал, — сигнал, а не мелочь

У `websockets.connect` `open_timeout` по умолчанию 15 с и оборачивает всё,
включая TCP-коннект. В логе попытки жили 53 с и ~200 с. Обернуть это вторым
`wait_for` — бесполезно: он сломается по той же причине, что и первый. Если
таймаут просрочен, проблема не в его отсутствии, а в том, что коду не давали
идти. Правильный ход — залогировать цифру и искать, кто держал цикл.

## Стенд, эмулирующий край неверно, доказывает что угодно

Фальшивый микрофон в `latency_probe.py` в паузах между фразами не отдавал
кадров совсем. Настоящая звуковая карта сыплет их ровным потоком независимо
от того, говорит человек или молчит. Из-за расхождения микрофонный гейт с его
хвостом тишины ни разу не вызвался, и я успел сделать вывод «дело не в VAD» —
ровно противоположный правде.

Проверять эмуляцию по вопросу «чем поток от подделки отличается, когда НИЧЕГО
не происходит»: края чаще всего врут именно в простое, а не под нагрузкой.

## Экономия на отправке ломает то, что считает получатель

Микрофонный гейт не слал тишину, экономя квоту. Но конец фразы определял VAD
на стороне Gemini — по полученной тишине. Выбросив её, клиент отнял у сервера
единственный признак «человек договорил», и Джарвис перестал отвечать вовсе.

Прежде чем фильтровать исходящий поток, спросить: что на той стороне считают
ПО ЭТИМ ЖЕ данным, кроме основного содержания. Тишина, пустые кадры и
heartbeat'ы часто и есть сигнал.

## Метрика может быть отрицательной, потому что измеряет не то

«Слышит» считалось от последнего громкого кадра до первого фрагмента
расшифровки — и выходило −900 мс. Расшифровка потоковая: фрагменты приходят,
пока человек ещё говорит. Отрицательное значение тут не баг вычитания, а
признак того, что у события нет той точки отсчёта, которую ему приписали.

## Прежде чем менять компонент — доказать, что тормозит именно он

Предложение было заменить синтез речи на более быстрый сторонний. Разложение
задержки показало: синтез ни при чём, 2.8 из 4.1 секунды съедали раздумья
модели, и лечится это одним параметром `thinking_budget=0`. Замена компонента
не дала бы ничего и стоила бы переписывания всего голосового тракта.

Общее правило: «X быстрый» — не довод, пока не показано, что медленный именно
тот участок, который X заменяет. Разложение задержки по стадиям стоит один
прогон стенда и экономит недели не туда.

## Интерфейс, реагирующий на random, — заставка, а не интерфейс

HUD «дышал» на `random.uniform()` и рисовал осциллограф из `random.randint()`:
одинаково в тишине и на крике. Выглядело живым ровно до первого взгляда в код.
Оба настоящих сигнала — RMS микрофона и амплитуда собственного голоса — уже
считались рядом и просто не доходили до холста.

Прежде чем добавлять эффекты, проверить, не выдумывает ли интерфейс данные,
которые у программы уже есть. Связь с реальностью даёт больше «дорогого»
ощущения, чем любое свечение.

Мелочь, которая решает: живой уровень брать быстро, отпускать плавно —
атака/затухание как у стрелочного индикатора. Симметричная реакция выглядит
механической.

## Кодировка консоли (повторно)

`print` с эмодзи в pytest под Windows валится UnicodeEncodeError в cp1251
и маскируется под провал теста. `PYTHONIOENCODING=utf-8` перед командой.

## Fail-closed по умолчанию для удалённого доступа

Если переменная `TELEGRAM_ALLOWED_USERS` или список `allowed_user_ids` пуст, система обязана запрещать доступ (Fail-Closed) и логировать предупреждение, а не открывать команды и мост ПК первому встречному. На `/pc-link` токен обязателен: отсутствие токена на сервере — повод закрыть порт, а не пускать кого угодно.

## Barge-in должен слышать динамики

Проверка микрофонного RMS для перебивания без учёта того, говорит ли в этот момент колонка (`SpeakerMeter`), приводит к самоперебиванию: микрофон ловит эхо голоса самого ассистента и мгновенно сбрасывает воспроизведение. При активных динамиках порог перебивания повышается, требуя осознанной и громкой речи пользователя рядом с микрофоном.

## Win32 keybd_event вместо powershell

Запуск процесса `powershell.exe -Command` для эмуляции громкости и мультимедиа-клавиш блокирует поток на 600–1500 мс на запуск процесса. Прямой вызов `ctypes.windll.user32.keybd_event` на Windows срабатывает за доли микросекунды без дочерних процессов.

## Единый резолвинг путей для сборки PyInstaller

В скомпилированном `.exe` пути вида `_BASE / "config" / "api_keys.json"` указывают в `_internal`, где пользовательские файлы отсутствуют или не имеют прав на запись. Все модули действий обязаны использовать `core.paths.get_config_path()`, умеющий находить конфиг в `%APPDATA%/JARVIS`.

## Строгая фильтрация Wake-Word против эффекта «открытого микрофона»

Автоматическое продление окна активности (`_wake_active_until = now + 10s`) после каждого ответа при непрерывном стриминге микрофона превращает ассистента в открытый микрофон: любые реплики домочадцев, разговоры в комнате или звук телевизора каждые несколько секунд продлевают окно и заставляют модель отвечать на всё подряд.
Правильное решение для десктопа: каждая реплика валидируется строго по наличию обращения («Джарвис...») через `is_addressed_to_jarvis()`. Горячая клавиша F8 активирует ровно одну реплику и сразу сбрасывается. Если обращения нет — буферы сбрасываются молча без вызова синтеза и инструментов.

## Spotify API: Device Type 'Computer' и запуск с рабочим каталогом

1. Официальный Spotify Web API для десктопных клиентов возвращает `type: 'Computer'`, а имя устройства равно имени хоста Windows (например, `SARDORBEK`). Фильтрация по `'desktop' in device_type` или `'spotify' in name` даёт `None` со 100% гарантией, из-за чего Web API падает в 404 (No active device) и ошибочно уходит в YouTube-фолбэк.
2. `Spotify.exe` на Windows использует библиотеки Chromium Embedded Framework (`Spotify.dll`, `libcef.dll`), лежащие рядом с исполняемым файлом. Запуск через `subprocess.Popen([exe])` без явного `cwd=os.path.dirname(exe)` приводит к немедленному завершению процесса из-за ненахождения DLL.
3. Локальный клиент Spotify поставляется с официальной утилитой `spotify_cli.exe` в `%APPDATA%\Spotify\`. Вызовы `spotify_cli play <uri>`, `pause`, `resume`, `next`, `previous`, `now-playing` работают через локальный IPC без задержек сети, не требуют токена и выполняются мгновенно.

## Gemini Live API: Коллизия 1011 (Internal Error) при вызове инструментов

В протоколе Gemini Live (двунаправленный WebSocket) непрерывный стриминг микрофона (`send_realtime_input`) во время обработки `tool_call` переводит автомат состояний Google в состояние гонки (race condition) и вызывает серверный сброс сессии с кодом `1011 (internal error)`.
Решение:
1. При получении `tool_call` немедленно выставлять флаг `_tool_in_progress = True` и очищать исходящую очередь аудио.
2. Возобновлять отправку аудио строго ПОСЛЕ отправки `send_tool_response`.
3. Во время воспроизведения сигнала активации (Chime, ~200 мс) игнорировать аудио-кадры микрофона, чтобы синтетический двухтональный писк не попадал в распознавание Gemini.
4. Ошибку 1011 переподключать мгновенно (за 0.3 сек) без спама на HUD пользователя «связь оборвалась» при штатных транзиентных реконнектах. Окно wake-word закрывается сразу после выполнения команды (если не был задан встречный вопрос '?'), полностью отсекая фоновые разговоры в комнате.

## Выбор микрофона: Системный Default против виртуальных утилит (ASUS AI Noise-cancelling)

В ноутбуках ASUS виртуальное устройство `AI Noise-cancelling Input (ASUS Utility)` может выдавать колоссальный паразитный шум (замеренный RMS > 8300 при нормальном микрофонном RMS ~280) либо агрессивно резать согласные и первые слоги.
Автоматический выбор аудиоустройства никогда не должен предпочитать сомнительные виртуальные DSP-фильтры системному устройству по умолчанию (`sd.default.device[0]` / `None`). В Windows все остальные программы (Telegram, Discord, браузер) используют системный Default (`Набор микрофонов (Realtek(R) Audio)`). Джарвис обязан в первую очередь подключаться к системному дефолту, а виртуальные утилиты отсекать, если пользователь явно не указал их в `MIC_DEVICE`.
Также задержка VAD (`VAD_SILENCE_MS`) не должна опускаться ниже 350-400 мс: значение 220 мс рубило живую речь на полуслове при естественных межсловных паузах человека.

## Потоковый синтез TTS и ранний арбитраж реплик
1. Разделение выходного текста на фразы (`_split_for_speech`) и асинхронный запуск генерации следующего фрагмента во время проигрывания текущего снижает задержку ответа до времени синтеза первого короткого предложения (~900 мс).
2. ~~Ранний арбитраж по первой порции `input_transcription`~~ — **ОПРОВЕРГНУТО 2026-09-10.** Расшифровка приходит кусками по слогу («Ка», «кая», « сегод»). По обрывку «Джар» реплика признавалась «не к Джарвису» и выбрасывалась целиком, по «включи сериал» стартовало уточнение, и договорённое название терялось. Арбитраж — только по полной фразе в `turn_complete`.

## Разбор «всё работает неправильно» (2026-09-10)
1. **Зелёные тесты врали, потому что проверяли заглушки.** В `movie_player`/`music_player` «для legacy-тестов» стояли `pyautogui = type(...)()`, `_focus_movie_player = lambda: True`, `browser_control = lambda: ...`; тесты патчили эти же заглушки. Перемотка и полный экран вживую не делали НИЧЕГО, отвечая «перемотал». Правило: запрещено подменять прод-реализацию ради теста — патчить края в самом тесте.
2. **Сначала живой стенд, потом чтение кода.** `scripts/latency_probe.py` (голос Irina + настоящий Gemini) за 2 минуты показал то, чего не видели 670 тестов: после «Джарвис» окно закрывалось в ту же миллисекунду, расшифровок было НОЛЬ.
3. **Холостой тик ≠ событие.** Блок «ответ закончен → STANDBY» в `_play_audio` висел на таймауте очереди (каждые 100 мс) без проверки, что Джарвис вообще говорил. Однократные переходы вешать на смену флага (`_is_speaking` True→False), не на тик.
4. **Нельзя одновременно отнять у модели инструменты и отдать их узким регуляркам.** `LOCAL_ACTION_TOOLS` вырезал из объявления Gemini фильмы/музыку/приложения/ПК/окна, а `_execute_tool` их ещё и блокировал. Всё, что не совпало с регуляркой, «выполнялось» словами. Двойное исполнение отсекается проверкой владения ходом, а не отъёмом инструментов.
5. **Адресованность — свойство реплики, а не таймера.** Слово «Джарвис» съедает локальный KWS, в расшифровку оно не попадает, а 8-секундное окно к `turn_complete` истекает. Флаг `PendingGeminiTurn.addressed` ставится в момент wake/хоткея.
6. **Сигнатуры между слоями.** `music_player` передавал объект `MediaRequest` в `play_media(raw_query: str)`; парсер пересоздавал его как фильм → музыка искалась на VK Видео (и из Telegram тоже).

## PyQt6 векторный HUD в стиле Тони Старка
1. Анимационные состоятельные свойства (`_boot_progress`, `_thinking_angle`, `_glitch_offset_x/y`) должны рассчитываться исключительно в 60 FPS цикле `_step()`, а рендериться в `paintEvent()`. Это предотвращает лаги GUI и обеспечивает плавный отклик интерфейса на события системы.
2. При сбое связи (`RECONNECTING`) смещение координатной сетки сканлиниями и мелким джиттером (`p.translate`) даёт точный физический эффект статических помех терминала Iron Man.


## 2026-09-15 — «полно ошибок после Gemini»: статика зелёная, ломалось живое
- Диск C: был забит на 100% — ни pytest, ни ruff не запускались. Сначала `Get-PSDrive C`, потом код.
- ruff 110 + 635 тестов зелёные ≠ работает. Стенд `scripts/latency_probe.py` дал 1/3 ответов. Правило прежнее: при «работает неправильно» — сначала стенд.
- Gemini подменяет рабочие функции лямбдами-заглушками, чтобы тесты прошли (`actions/music_player.py`: `_spotify_search_track_uri = lambda q: None`). Провайдер Spotify после этого рапортовал успех, ничего не включив. Проверка: `grep -rn "= lambda" actions core`. Настоящие хелперы теперь в `actions/spotify_desktop.py`.
- `wttr.in` умер (http=000) — погода на Open-Meteo, как в TG-боте.
- Окно шума комнаты наполнялось всеми кадрами STANDBY, а «Джарвис, …» говорят ИМЕННО в STANDBY → порог речи 12→6190, микрофон глох. Теперь в фон идут только кадры с p(речь) < 0.2; без Silero — медиана вместо p90.
- Вето wake-word сравнивало с окном вероятностей, которое не обновлялось во время SPEAKING → отбрасывало реальное обращение по протухшей тишине. Окно старше 0.75 с не голосует.
- Тесты `test_window_focus_safety` и `test_browser_bridge_unavailable_and_fallback_activation` ходили по РЕАЛЬНОМУ рабочему столу и жали пробел в открытом браузере разработчика (флаки: зависят от открытых окон). Любой тест BrowserMediaController обязан мокать `is_window_valid_and_targeted`.
- Fish TTS (`s2.1-pro-free`) плавает от 0.9 до 4 с на фрагмент в течение одного вечера — это сервер, не наш код. Добавлен лог «фрагмент #n синтезирован за X мс», чтобы больше не гадать.
- Fish отдаёт звук потоком, но `speak_pcm` ждал весь файл. Теперь `tts_fish.stream_pcm` + один общий генератор `_iter_fragment_audio` на оба пути озвучки (стрим и `_speak_fish`). Вечером 16.09 Fish отдавал первый байт через 11 с — введён хедж `JARVIS_TTS_FIRST_AUDIO_TIMEOUT` (3 с): параллельно поднимается Edge, кто первый — тот говорит; пустой Edge (NoAudioReceived) → ждём Fish. Предзаказ первой фразы идёт через тот же хедж.
- Нельзя `aclose()` асинхронный генератор, пока его `__anext__` в задаче: сначала `cancel()` + `gather(return_exceptions=True)`.
- Тесты с фиксированным `sleep(0.05)` на фоновую задачу флакают (первый импорт edge_tts дороже) — ждать условие в цикле.
- Состояние THINKING существовало, но не использовалось: после арбитража машина стояла в LISTENING, тикал таймер тишины и шлюз ронял её в STANDBY до ответа. Теперь `_enter_thinking()` при передаче хода Gemini; сторож `THINKING_MAX_SEC` (40 с) в audio_pipeline, чтобы оборванный ход не оставил Джарвиса глухим; пустой ход модели → STANDBY.
- Метрика «слышит» считалась от ПЕРВОГО фрагмента расшифровки (они идут по слогам во время речи) → минус. Точка — последний фрагмент.
- Стенд latency_probe гасит сессию сразу после третьего замера — «SPEAKING -> STANDBY» на последней реплике это остановка стенда, а не потеря follow-up.
- Кэш голоса (`core/voice_cache.py`) восстановлен: фиксированные фразы — с диска за ~1 мс. Ключ включает голос и частоту; в кэш только Fish, Edge никогда. Прогрев — фоновый поток после подключения, с паузой между фразами и остановкой после 3 отказов.
- Barge-in: `turn_complete` прерванного хода сбрасывал `_interrupted_turn` первой строкой и озвучивал старый ответ заново целиком. Теперь прерванный ход закрывается молча (out_buf/in_buf/очередь чистятся). Тест `test_прерванный_ход_не_озвучивается_заново`.
- Время/дата/текущая погода — локально в FastCommandRouter (§23–24, `actions/local_answers.py`), без модели: стенд 6–12 с → 2,7–3,6 с (остаток = распознавание речи ~1 с + Fish). Прогноз («завтра», «на неделю») намеренно оставлен модели. Геокодер Open-Meteo понимает только именительный падеж → `city_candidates()` снимает окончания, плюс псевдонимы ослышек и difflib по известным городам; координаты кэшируются.
- Локальный ответ — тоже ход диалога: выставлять `_followup_timeout`, иначе после «какая погода» окно продолжения не открывалось.
- Мост к браузеру (extension/) никогда не работал: порт 8765 vs 18765, токен расширению неоткуда взять. Теперь расширение опознаётся по Origin `chrome-extension://` (страница подделать не может), токен — второй способ. Управление видео без расширения = хоткеи в окно, хрупко.
- Переключение «музыка → фильм»: глушить активную сессию ДО поиска нового (`preempt_active_session`), иначе секунды играют оба. Spotify при смене — пауза (`on_superseded`), не taskkill. Музыка через Web API (`play_music`) обязана регистрировать сессию, иначе переключение её не видит.
- Media-клавиша play/pause — переключатель; для Spotify пауза/продолжение через Web API (детерминировано), клавиша — фолбэк.
- Расширение моста ставить в БРАУЗЕР ПО УМОЛЧАНИЮ (у владельца Яндекс.Браузер, `browser://extensions`, «Режим разработчика» внизу страницы), не в Chrome. MV3 service worker засыпает через ~30 с — переподключение только через `chrome.alarms` + события вкладок, `setTimeout` теряется.
- Команда мосту без tab_id уходила «во все вкладки», и в этой ветке расширение не отвечало → 2 с таймаута на каждую паузу. Контроллер резолвит вкладку провайдера по URL (`_resolve_tab_id`), расширение отвечает и на широковещание.
- Spotify Web API управления плеером (`/me/player/*`) без Premium = 403. Адресная пауза/play через Windows GSMTC (`media_session_manager.control_app("spotify", …)`) работает без API за ~20 мс; 403 запоминается, дальше API не дёргается.
- `fuzz.partial_ratio` с порогом 60 по своим плейлистам: «daft punk get lucky» → плейлист «funk» (по «punk»). Свой плейлист только при token_set_ratio ≥ 85.
- Тесты, поднимающие BrowserBridgeServer, обязаны жить на эфемерном порту (`JARVIS_BRIDGE_PORT=0` в conftest), иначе к ним подключается настоящее расширение и приносит живые вкладки.
- Подтверждения (2026-09-16): очевидные действия (громкость, пауза, трек, фильм, музыка, приложение) — только earcon, слова — на ошибки, уточнения и невидимые действия (таймер, напоминание, заметка). `core/confirmations.py`: `should_stay_silent`, `is_bare_confirmation` (голое «Готово. Сделано, сэр.» модели после инструмента → сигнал). Провал быстрой команды ОЗВУЧИВАЕТСЯ (раньше молчал, как и успех).
- Расшифровка Gemini пишет имя как «Жарвиз», «Джарвиз», «Джарвист» — единый список `core/wake_names.py` для fast-path, слот-филлера и арбитража; без него команды уезжали в модель на 6–7 с.
- Сервер моста поднимать при старте (`_start_browser_bridge`), не лениво при первом фильме — иначе расширение подключается на 3 с позже и первый фильм идёт через хоткеи.
- Стенд: `JARVIS_PROBE_PHRASES="фраза|фраза"` гоняет любой голосовой сценарий, в т.ч. команды.
- Строгий режим обращения (`core/wake_policy.py`, JARVIS_STRICT_WAKE=1 по умолчанию): имя обязательно; окно без имени только после вопроса/уточнения Джарвиса (6 с) и после «не разобрал, повторить?» (2 с); RMS-перебивание выкл; после перебивания по громкости шлюз не открывается; Vosk — только финальные результаты и точное слово; wake + расшифровка ≤ 8 с = одна реплика, таймер тишины без речи снимает `addressed`. Расшифровка от Gemini приходит через 3–5 с ПОСЛЕ слова — зазор 2.5 с отбрасывал легитимные команды.
- Стенд: WAV фраз именовать по хэшу текста — `p0..p5` подхватывали старый сценарий.
- Локальный STT команд (`core/local_stt.py`, Vosk полный словарь, та же модель что у KWS): итог через ~0.1 с после конца речи → «который час» слышит 200 мс / ответ 0.9–1.2 с (было 2.7–3.6). Ранний арбитраж `_arbitrate_turn(local_only=True)`: только локальные роутеры, не-команда остаётся Gemini (его расшифровка точнее на названиях). Предбуфер при пробуждении ОБЯЗАН идти и в STT — без него терялось первое слово («громче» вместо «сделай громче»).
- После локального исполнения нельзя обрывать поток в Gemini посреди хода: `CLOUD_TURN_DRAIN_SEC`=1.2 с цифровой тишины в STANDBY, иначе VAD Gemini дослушивал старую фразу со следующим «Джарвис» и исполнял команду второй раз.
- Стенд-артефакт: фальшивый микрофон (WAV) + настоящий loopback колонок → при играющем фильме AEC коверкает следующую синтетическую фразу. Не баг тракта.
- Своя wake-модель «Джарвис» (`scripts/wake_data_gen.py` → Silero TTS 5 голосов + SAPI Irina; `scripts/record_wake_samples.py` → голос владельца; `scripts/train_wake_word.py` → MLP поверх эмбеддера openWakeWord, экспорт `config/wake_models/jarvis_ru.onnx` + .json с порогом). Данные в `data/wake/` (gitignore, голос владельца — НЕ коммитить; без них переобучить нельзя).
- Грабли обучения: (1) Silero TTS отдаёт 8/24/48 кГц; (2) эмбеддеру нужно ~2 с на 16 кадров; (3) записи владельца ×40 аугментаций без негативов его голосом → модель учит «этот микрофон = Джарвис» — нужны негативы тем же голосом и шум его микрофона в синтетике; (4) главное — обучать скользящими окнами «как в потоке» (позитив только когда слово ТОЛЬКО ЧТО закончилось; полслова и начало чужой речи — негативы), иначе срабатывает на начало любой фразы.
- В детекторе сеть обязана получать ВСЕ кадры (её потоковый спектрограф непрерывный); срабатывание — по двум кадрам подряд над порогом. Порог из .json не должен перекрывать явно заданный. Вето Vosk по слову-приманке приходит позже пика сети — по времени бесполезно.
- Метрика на голосе владельца (offline, бегущее окно): 21/25 ловит, 1/38 ложных на трудных словах (сервис/жалюзи/Дарвин); Vosk-only в строгом режиме — 7/25.
- Слух, шаг 3 (WebRTC APM) отложен: на машине нет MSVC, Windows-колёс нет. Шаг 4 (хвост тишины) НЕ даёт выигрыша: после речи в облако и так идёт цифровая тишина, конец фразы решает Silero (0.3 с) — локальные команды уже через ~0.2 с.
- Голосовой отпечаток (`core/speaker_verifier.py`, Resemblyzer, JARVIS_OWNER_ONLY=1, выкл по умолчанию): банк эмбеддингов записей владельца (`scripts/enroll_owner_voice.py` → config/owner_voice.npy, gitignore), сходство = среднее по 3 ближайшим, порог 0.62 (среднее-эталон разделял хуже: чужой 0.61 vs свой 0.62). Проверка в конце фразы в AudioPipeline ДО локального роутера; чужой голос → `_on_foreign_voice` закрывает шлюз и помечает реплику DISCARDED — иначе Gemini принимал её по слову «Джарвис» в тексте.
- Падение 0xc0000005 в ntdll после перебивания по слову: чирп пробуждения и щелчок barge-in стартовали из двух потоков через `sounddevice.play()`, который держит ОДИН глобальный поток и закрывает предыдущий, пока тот играет. Все короткие звуки — через `core/short_sounds.play_blocking` под общим замком. Диагностика нативных падений: faulthandler → logs/crash.log, Windows Application log (APPCRASH).
- Wake-модель 1.00 на тихом шуме комнаты: синтетика без реального фона. Лечение — записи комнаты (data/wake/room) как негативы + страховка по громкости (`_loud_enough_for_speech`). Silero-вето для нейросети снято: при играющих колонках оно резало настоящее слово.
- ГЛАВНАЯ петля ложных пробуждений (17.09): `return` на cooldown в детекторе оставлял дыру 1.2 с в потоке для сети с непрерывным буфером спектра → мусорные признаки → 1.00 → срабатывание → снова cooldown. Офлайн не воспроизводилось (стенд не имел серии срабатываний). Модели кормить ВСЕГДА, cooldown запрещает только срабатывание. Диагностика: сравнивать дампы того же звука офлайн vs живой балл.
- Расшифровка Gemini пишет имя как «Чарли/Чарлись/Чарльз» — строковая похожесть не спасает (сервис ближе к джарвис). Правило позиционное `strip_vocative`: детектор подтвердил слово + фраза начинается с одного слова и запятой → снять. Gemini отвечал время ПО ПАМЯТИ («как и докладывал ранее») — добавлен инструмент `get_time`.
