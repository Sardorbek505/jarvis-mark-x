"""Папка пользовательских плагинов.

Один файл — одна способность. Положили `.py` с объявлением `PLUGIN` — при
следующем запуске ДЖАРВИС умеет новое; убрали файл — перестал о нём заявлять.
Образец лежит в `_template.py`: файлы, начинающиеся с подчёркивания, реестр
пропускает, поэтому шаблон не превращается в инструмент сам по себе.
"""
