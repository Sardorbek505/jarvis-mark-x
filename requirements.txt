# ДЖАРВИС — Зависимости
# pip install -r requirements.txt

# Ядро Gemini Live
google-genai>=1.0.0

# UI
PyQt6>=6.6.0
Pillow>=10.0.0

# Аудио
sounddevice>=0.4.6
numpy>=1.24.0

# Локальное распознавание ключевого слова «Джарвис» (2-Stage KWS).
# Без vosk и openwakeword детектор не поднимается, и Джарвис реагирует
# только на горячую клавишу — это не опциональные пакеты.
vosk>=0.3.45
openwakeword>=0.6.0
onnxruntime>=1.16.0

# Эхоподавление и приглушение фона (только Windows).
# pyaudiowpatch даёт WASAPI loopback — опорный сигнал колонок для AEC,
# без него эхоподавление не выполняется; pycaw управляет громкостью сессий.
pyaudiowpatch>=0.2.12.5; sys_platform == "win32"
pycaw>=20230407; sys_platform == "win32"
comtypes>=1.2.0; sys_platform == "win32"
# soxr — потоковый ресэмплер loopback (192 кГц) → 16 кГц с состоянием между
# блоками. Поблочный linspace-ресэмплинг давал копию музыки с корреляцией 0.29
# к оригиналу: вычитать было нечего и AEC не сходился. Без soxr код откатится
# на грубый путь и напишет об этом в лог.
soxr>=0.3.7

# Системный мониторинг
psutil>=5.9.0

# Веб-камера — снимок с камеры ПК (/camera)
opencv-python>=4.8.0

# Vision Mode — захват экрана и определение активного окна
mss>=9.0.0
pygetwindow>=0.0.9

# Movie Player — отправка клавиш в плеер (опционально, есть PowerShell fallback на Windows)
pyautogui>=0.9.54

# Selenium для автоматического выбора фильмов (надёжнее чем pyautogui keyboard navigation)
selenium>=4.15.0
webdriver-manager>=4.0.0

# Google Calendar API (опционально для синхронизации календаря)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# News Aggregator - RSS фиды для новостей
feedparser>=6.0.0

# Spotify API - requests и fuzzy matching
requests>=2.31.0
rapidfuzz>=3.0.0

# Telegram Bot + Mini App (опционально — только для telegram_bot/)
python-telegram-bot>=20.0
websockets>=12.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
aiosqlite>=0.19.0
edge-tts>=6.1.0
pydub>=0.25.1
imageio-ffmpeg>=0.4.8
