# ДЖАРВИС — Зависимости
# pip install -r requirements.txt

# Ядро Gemini Live
google-genai>=1.0.0

# UI
PyQt6>=6.6.0
Pillow>=10.0.0

# Аудио
sounddevice>=0.4.6

# Системный мониторинг
psutil>=5.9.0

# Приглушение музыки при речи и замер звука колонок (Windows CoreAudio).
# Без них обе функции молча выключены: музыка из колонок уходит в Gemini.
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.2; sys_platform == "win32"

# Веб-камера — снимок с камеры ПК (/camera)
opencv-python>=4.8.0

# Vision Mode — захват экрана и определение активного окна
mss>=9.0.0
pygetwindow>=0.0.9

# Movie Player — отправка клавиш в плеер (опционально, есть PowerShell fallback на Windows)
pyautogui>=0.9.54

# Selenium для автоматического выбора фильмов (надёжнее чем pyautogui keyboard navigation)
selenium>=4.15.0
webdriver-manager>=4.0.0

# Google Calendar API (опционально для синхронизации календаря)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# News Aggregator - RSS фиды для новостей
feedparser>=6.0.0

# Spotify API - requests и fuzzy matching
requests>=2.31.0
rapidfuzz>=3.0.0

# Telegram Bot + Mini App (опционально — только для telegram_bot/)
python-telegram-bot>=20.0
websockets>=12.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
aiosqlite>=0.19.0
edge-tts>=6.1.0
pydub>=0.25.1
imageio-ffmpeg>=0.4.8

# Слово «Джарвис» офлайн на ПК (core/wake_vosk.py). Модель качает сборка.
vosk>=0.3.45

# Яркость: ноутбук (WMI) и внешние мониторы (DDC/CI)
screen_brightness_control>=0.24; sys_platform == "win32"

# Поиск без ключей: трек Spotify, страница фильма, факты (core/web_find.py)
ddgs>=9.0

# Медиа-сессии Windows: явные play/pause/next и «что играет» (core/media_session.py)
winrt-runtime>=2.0; sys_platform == "win32"
winrt-Windows.Foundation>=2.0; sys_platform == "win32"
winrt-Windows.Foundation.Collections>=2.0; sys_platform == "win32"
winrt-Windows.Media.Control>=2.0; sys_platform == "win32"

# Джарвис звонит в Telegram (core/tg_call.py) и шлёт сообщения от вашего
# имени (telegram_bot/pc_userbot.py). Telethon раньше не было в списке —
# userbot в сборке не мог работать вовсе.
telethon>=1.36
py-tgcalls==3.0.0
# QR-код для входа в аккаунт Джарвиса без кода (core/tg_call.py).
qrcode>=7.4
